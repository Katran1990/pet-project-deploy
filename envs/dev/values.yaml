# dev environment. The `tag` fields are rewritten by CI after every merge
# into `development`; everything else you edit by hand.
backend:
  tag: d012151
frontend:
  tag: d012151
ingress:
  host: dev.pet.local
